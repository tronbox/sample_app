<%= form_for(@series) do |f| %>
  <%= render 'error_messages', target: @series %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
